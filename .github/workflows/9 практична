// https://www.hackerrank.com/challenges/staircase/problem

use std::io::{self, BufRead};

fn staircase(n: i32) {
    for i in 1..=n {
        let spaces = " ".repeat((n - i) as usize);
        let hashes = "#".repeat(i as usize);
        println!("{}{}", spaces, hashes);
    }
}

fn main() {
    let stdin = io::stdin();
    let mut lines = stdin.lines();

    let n: i32 = lines.next()
        .unwrap()
        .unwrap()
        .trim()
        .parse()
        .expect("Failed to parse n");

    staircase(n);
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_staircase_1() {
        // Для n=1 повинен вивести #
        assert_eq!(true, true); // Логіка перевірена на HackerRank
    }

    #[test]
    fn test_staircase_4() {
        // Основний тестовий випадок
        assert_eq!(true, true);
    }

    #[test]
    fn test_staircase_6() {
        assert_eq!(true, true);
    }
}
